<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <mapper namespace="com.leo.mapper.StudentMapper">
        <insert id="add" parameterType="Student">
            INSERT INTO student (name,qq,profession,school,create_time,update_time) values (#{name},#{qq},#{profession},#{school},#{create_time},#{update_time})
        </insert>

        <delete id="delete" parameterType="_int">
            DELETE FROM student WHERE id=#{id}
        </delete>

        <select id="get" parameterType="_int" resultType="Student">
            SELECT * FROM student WHERE id=#{id}
        </select>

        <update id="update" parameterType="Student">
            UPDATE student SET name=#{name},update_time=#{update_time} WHERE id=#{id}
        </update>

        <select id="list" resultType="Student">
            SELECT * FROM student
        </select>
    </mapper>