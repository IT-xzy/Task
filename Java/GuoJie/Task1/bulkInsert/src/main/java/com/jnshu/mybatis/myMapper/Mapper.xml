<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jnshu.mybatis.myMapper.Mapper">

    <!-- 需求：通过id查询用户 -->
    <select id="findUserById" parameterType="int" resultType="User">
        select *  from demo where id=#{id}
    </select>
    <select id="findUserList" parameterType="com.jnshu.mybatis.user.User" resultType="User">
        select * from demo
        <where>
            <if test="id=!null">
                id in
                <foreach collection="idList" item="id" open="(" close=")" separator=",">
                 #{id}
                </foreach>
            </if>
        </where>
    </select>
    <insert id="insertUser" parameterType="com.jnshu.mybatis.user.User">
        insert demo
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id!=null">id,</if>
            <if test="name!=null">name,</if>
            <if test="email!=null">email</if>
        </trim>
        <trim prefix="values(" suffix=")" suffixOverrides=",">
            <if test="id!=null">#{id,jdbcType=INTEGER},</if>
            <if test="name!=null">#{name,jdbcType=VARCHAR},</if>
            <if test="email!=null">#{email,jdbcType=VARCHAR},</if>
        </trim>
    </insert>
    <insert id="insertList" parameterType="User">
        insert into demo values
        <foreach collection="userList" item="user" separator=",">
            (#{user.id},#{user.name},#{user,email})
        </foreach>
    </insert>
</mapper>