<%@ taglib prefix="json" uri="http://www.atg.com/taglibs/json" %>
<json:object>
    <json:property name="name" value="${user.name}"/>
    <json:property name="id" value="${user.id}"/>
    <json:property name="qq" value="${user.qq}"/>
    <json:property name="age" value="${user.age}"/>
    <json:property name="channel" value="${user.channel}"/>
    <json:property name="create_at" value="${user.create_at}"/>
    <json:property name="desire" value="${user.desire}"/>
    <json:property name="enrolmentTime" value="${user.enrolmentTime}"/>
    <json:property name="school" value="${user.school}"/>
    <json:property name="tid" value="${user.tid}"/>
    <json:property name="update_at" value="${user.update_at}"/>
</json:object>

