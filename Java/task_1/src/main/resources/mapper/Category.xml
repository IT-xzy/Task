<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 为这个mapper指定一个唯一的namespace，namespace的值习惯上设置成包名+sql映射文件名，这样就能够保证namespace的值是唯一的
 例如namespace="mapper.Category"就是mapper(包名)+Category(Category.xml文件去除后缀)
-->
<mapper namespace="mapper.Category">
    <!-- 在select标签中编写查询的SQL语句， 设置select标签的id属性为getCategory，id属性值必须是唯一的，不能够重复
       使用parameterType属性指明查询时使用的参数类型，resultType属性指明查询返回的结果集类型
        resultType="student"就表示将查询结果封装成一个User类的对象返回
        User类就是users表所对应的实体类
        -->
    <insert id="addCategory" parameterType="student" >
        insert into student ( name ) values (#{name})
    </insert>
    <delete id="deleteCategory" parameterType="student">
        delete from student where id=#{id}
    </delete>
    <select id="getCategory"  parameterType="_int" resultType="student">
        select * from student where id = #{id}
     </select>
    <update id="updateCategory" parameterType="student">
        update student set name = #{name} where id = #{id}
    </update>

    <select id="listStudent" resultType="student">
        select * from   student
    </select>

</mapper>
