<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<!-- 参考官网：http://www.mybatis.org/mybatis-3/zh/configuration.html#settings -->
<configuration>
    <!-- 全局配置 -->
    <settings>
        <!-- 使用列标签代替列名 -->
        <setting name="useColumnLabel" value="true"/>
        <!--不允许 JDBC 支持自动生成主键-->
        <setting name="useGeneratedKeys" value="false"/>
        <!--是否开启自动驼峰命名规则（camel case）映射，即从经典数据库列名 A_COLUMN 到经典 Java 属性名 aColumn 的类似映射。 -->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
        <!--指定日志类型为log4j-->
        <setting name="logImpl" value="LOG4J"/>
        <!-- 打开延迟加载的开关 -->
        <setting name="lazyLoadingEnabled" value="true"/>
        <!-- 将积极加载改为消极加载，即延迟加载 -->
        <setting name="aggressiveLazyLoading" value="false"/>
        <!--设置懒加载的触发方式-->
        <!--默认--><!--<setting name="lazyLoadTriggerMethods" value="equals,clone,hashCode,toString,..." />-->
        <!--<setting name="lazyLoadTriggerMethods" value="equals,clone,hashCode,get*"/>-->
    </settings>
</configuration>