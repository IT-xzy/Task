//返回游戏主页按钮
$("a.closeButton").click(
    function () {
        sessionStorage.clear();
            sessionStorage.clear();
            location.href = "playerDistribution.html"
        
    }
)